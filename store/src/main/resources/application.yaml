server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/store
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  main:
    banner-mode: off
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  data:
    redis:
      host: localhost
#      host: host.docker.internal # dùng localhost cũng được, nhưng đây là redis trong docker nên dùng host.docker.internal cho rõ ràng
      port: 6379
      password: "StrongPass123!"
      database: 0
      timeout: 5s
      lettuce:
        pool:
          enabled: true
          max-active: 16
          max-idle: 8
          min-idle: 0
          max-wait: 2s
  mail:
    host: smtp.gmail.com
    port: 587
    username: tutruongno1@gmail.com
    password: "mlwo tnqu fevq jkgk"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
logging:
  level:
    root: info


jwt:
  signerKey: "Ipw5iOxpnL0VXwmcTK7OQHjLR5g/gHjB9ispYoUp+RY+yjksxO2jYEuf+asL9LQ1"
  access-duration: 3600      # seconds
  refresh-duration: 2592000 # seconds
cloudinary:
  cloud-name: dsgftzhzt
  api-key: 363524259282383
  api-secret: Fgg6RVnlQNtIe1cfAgg5sKORENw

management:
  endpoints:
    web:
      exposure:
        include: health,info

# SePay Payment Gateway Configuration
# Thay đổi các giá trị dưới đây theo thông tin tài khoản SePay và ngân hàng của bạn
sepay:
  api-key: "YOUR_SEPAY_API_KEY"           # API key từ SePay dashboard (https://my.sepay.vn)
  webhook-secret: ""                       # Webhook secret từ SePay (dùng HMAC-SHA256 verify)
                                           # Nếu để trống → fallback dùng api-key làm secret
  bank-code: "MBBank"                      # Mã ngân hàng (MBBank, VCB, TCB, ACB, ...)
  bank-account-number: "0123456789"        # Số tài khoản ngân hàng
  bank-account-name: "NGUYEN VAN A"        # Tên chủ tài khoản
  bank-name: "MB Bank"                     # Tên hiển thị ngân hàng